{
  "name": "@gun/core",
  "version": "0.9.9991",
  "description": "Core of the realtime, decentralized, offline-first, graph data synchronization engine.",
  "main": "src/index.js",
  "browser": "dist/gun.js",
  "scripts": {
    "prepublish": "npm run build",
    "test": "mocha",
    "build": "node ../../script/unbuild.js && rm -rf dist && rollup --config && uglifyjs dist/gun.js -o dist/gun.min.js -c -m"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amark/gun.git"
  },
  "keywords": [
    "gun",
    "gunDB"
  ],
  "engines": {
    "node": ">=0.8.4"
  },
  "author": "Mark Nadal",
  "license": "(Zlib OR MIT OR Apache-2.0)",
  "bugs": {
    "url": "https://github.com/amark/gun/issues"
  },
  "homepage": "https://github.com/amark/gun#readme",
  "dependencies": {},
  "devDependencies": {
    "mocha": ">=3.2.0",
    "rollup": "^0.61.1",
    "rollup-plugin-commonjs": "^9.1.3",
    "rollup-plugin-node-resolve": "^3.3.0",
    "uglify-js": "^3.4.1"
  }
}
